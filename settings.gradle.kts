pluginManagement {
    repositories {
        mavenCentral()
        maven {
            url = uri("https://repo.screamingsandals.org/public/")
        }
        gradlePluginPortal()
    }
}

rootProject.name = "VelocityVanish"
